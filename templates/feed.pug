div.container.px-0.mb-4
  ul.list-inline.row
    each val in coffeegrams
      li.list-unstyled.col-sm-4.mb-4
        img.img-fluid.rounded(src=`/uploads/${val.image}`).mb-2
        br
        strong #{val.coffeeType}
        br
        = val.description
        br
        small #{val.createdAt}
